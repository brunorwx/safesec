# Updates

Production updates must verify signed artifacts, preserve recordings, support rollback, and never replace device credentials during an application update. The current Docker image is a development packaging baseline, not a production update mechanism.
